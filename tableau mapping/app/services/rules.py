# Update: 2026-04-09 - Deployment trigger for Measures, Dimensions, and LODs
ALL_PROMPTS_TABLEAU_TO_DAX = [
    # -------------------------------------------------------------------------
    # DIMENSIONS — Calculated Columns (Row-level) — D1 to D7
    # -------------------------------------------------------------------------
    {"type": "dimensions", "id": "D1", "description": "STRICT COLUMN QUALIFICATION & FORMATTING: Resolve EVERY table column to exact DAX format 'TableName'[ColumnName] with single quotes around the table name. Deduce the TableName from the schema context provided; if uncertain, omit the table prefix entirely. CRITICAL: If a field is listed in 'Available Measures' or 'LOD EXPRESSIONS', it is a MEASURE and MUST NOT have a table prefix (use [MeasureName] only). Output clean DAX with ZERO comments. Use VAR ... RETURN for complex logic."},
    {"type": "dimensions", "id": "D2", "description": "LOGIC, NULLS & PARAMETERS: Translate conditional logic (IF/THEN/ELSEIF, CASE WHEN) to DAX IF() or SWITCH(TRUE(), ...). Map ISNULL() -> ISBLANK(), IFNULL() -> COALESCE(), ZN() -> COALESCE(x,0). PARAMETER RULES: (1) ALWAYS format parameter references using SELECTEDVALUE('ParameterTableName'[ParameterColumn]). Use the EXACT Table and Column name specified in the Available Parameters context. For DATATABLE-based parameters, the column name is ALWAYS 'Value' — use SELECTEDVALUE('ParameterTableName'[Value]). For DISTINCT-based parameters, the column name is derived from the source column — follow the schema context exactly. (2) When comparing a numeric column to a parameter value, wrap the parameter in INT() or VALUE() for type safety. (3) Every SWITCH() MUST include a default/else value as the last argument. Every IF/ELSEIF chain MUST end with a final ELSE clause."},
    {"type": "dimensions", "id": "D3", "description": "STRINGS, NUMBERS & DATES: Map string/numeric functions (LEFT, SPLIT, CONTAINS, ROUND, INT, etc.) to DAX equivalents. Date guard: DO NOT wrap existing date fields in DATE(); use the column directly. Reconstruct MAKEDATE-style fields using DATE(). DATEPART returns a NUMBER — map to MONTH(), YEAR(), DAY() directly without aggregation. DATENAME returns a TEXT name — map to FORMAT() with appropriate format string."},
    {"type": "dimensions", "id": "D4", "description": "SINGLE-VALUE SEMANTICS: ATTR([Col]) -> SELECTEDVALUE('Table'[Col]) in measure contexts. For row-level single-value contexts, emit the column directly."},
    {"type": "dimensions", "id": "D5", "description": "SEMANTIC ROLE: Set Default Summarization to 'Do not summarize' for date-parts and categorical dimensions in rule descriptions."},
    {"type": "dimensions", "id": "D6", "description": "STRING FORMATTING GUARD: When translating Tableau string conversions (STR, ROUND) for display, NEVER use 'General Number' as a FORMAT pattern — it is INVALID in DAX. Use FORMAT([Measure], \"0\") for whole numbers, FORMAT([Measure], \"0.00\") for decimals, FORMAT([Measure], \"0.00%\") for percentages. Use & for string concatenation."},
    {"type": "dimensions", "id": "D7", "description": "BOOLEAN COMPARISON GUARD: When a calculation compares a column to a parameter to produce a boolean result, and it is implemented as a Calculated Column, you MUST use the parameter's current/default value directly in the comparison since SELECTEDVALUE() returns blank in row context. If it is a Measure, use SELECTEDVALUE() for the parameter and ensure the table column is wrapped in a scalar aggregator. Avoid hardcoded literal values in the DAX output and prefer dynamic schema references."},
    {"type": "dimensions", "id": "D8", "description": "COLUMN IDENTITY PRESERVATION: NEVER rename, alias, or translate column names. If a column is listed as 'PowerBIName (Source: TableauName)', the identity is 'PowerBIName'. You MUST use 'PowerBIName' ONLY in the DAX output. DO NOT include the '(Source: ...)' part or anything inside the parentheses—this is a mapping hint, NOT part of the name. Inclusion of the '(Source: ...)' string or the TableauName in the DAX is a CRITICAL FAILURE. Every column name in the DAX output MUST match the target identity from the Schema Context exactly—no synonyms, no abbreviations, no expanded forms. This applies to ALL columns without exception."},
    {"type": "dimensions", "id": "D9", "description": "RESERVED KEYWORD GUARD FOR VARIABLE NAMES: NEVER use DAX or Power BI reserved keywords as VAR names. The following words are STRICTLY FORBIDDEN as variable names: TRUE, FALSE, BLANK, NOT, AND, OR, IN, VAR, RETURN, EVALUATE, ORDER, BY, ASC, DESC, DEFINE, MEASURE, COLUMN, TABLE, ROW, DATATABLE, CURRENCY, DATETIME, DOUBLE, INTEGER, STRING, BOOLEAN, DENSE, SKIP, EARLIER, EARLIEST, VALUE, DAY, MONTH, YEAR, HOUR, MINUTE, SECOND, IF, SWITCH, CALCULATE, FILTER, ALL, ALLSELECTED, ALLEXCEPT, REMOVEFILTERS, VALUES, DISTINCT, SELECTEDVALUE, HASONEVALUE, HASONEFILTER, ISBLANK, ISCROSSFILTERED, ISFILTERED, ISEMPTY, UNION, INTERSECT, EXCEPT, SUMMARIZE, ADDCOLUMNS, SELECTCOLUMNS, TOPN, GENERATE, GENERATESERIES, CROSSJOIN, NATURALINNERJOIN, NATURALLEFTOUTERJOIN, TREATAS, USERELATIONSHIP, RELATED, RELATEDTABLE, LOOKUPVALUE, RANK, RANKX, ROWNUMBER, OFFSET, WINDOW, INDEX, PARTITION, MATCHBY, ORDERBY, ABS, CEILING, FLOOR, ROUND, ROUNDUP, ROUNDDOWN, TRUNC, SIGN, SQRT, POWER, MOD, DIVIDE, SUM, SUMX, AVERAGE, AVERAGEX, MIN, MINX, MAX, MAXX, COUNT, COUNTA, COUNTX, COUNTROWS, COUNTBLANK, DISTINCTCOUNT, DISTINCTCOUNTNOBLANK, PRODUCT, PRODUCTX, CONCATENATE, CONCATENATEX, FORMAT, LEFT, RIGHT, MID, LEN, UPPER, LOWER, TRIM, SUBSTITUTE, REPLACE, REPT, SEARCH, FIND, EXACT, FIXED, UNICHAR, UNICODE, CODE, COMBINEVALUES, CONTAINSSTRING, CONTAINSSTRINGEXACT, SELECTEDMEASURE, SELECTEDMEASURENAME, ISSELECTEDMEASURE, SELECTEDMEASUREFORMATSTRING. Instead, use descriptive, prefixed names such as _TopN, _RankVal, _FilterVal, _Result, _CurrentValue, etc."},
    {"type": "dimensions", "id": "D10", "description": "CROSS-TABLE REFERENCE GUARD (CRITICAL DECISION TREE): When creating a Calculated Column (Dimension) on a TARGET TABLE that references a column from a SOURCE TABLE, you MUST follow this logic: 1. SAME TABLE: If Target and Source are the exact same table, use the column directly: 'Table'[Column]. NEVER use RELATED() or RELATEDTABLE(). 2. MANY-TO-ONE LOOKUP: If Target is the 'Many' side and Source is the 'One' side (e.g., looking up a dimension value from a fact table), you MUST use RELATED('SourceTable'[Column]). This is the most common scenario for dimensional lookups. 3. ONE-TO-MANY AGGREGATION: Only if Target is the 'One' side and Source is the 'Many' side, use an aggregator: MAXX(RELATEDTABLE('SourceTable'), 'SourceTable'[Column]). Consult 'AVAILABLE RELATIONSHIPS' to determine cardinality!"},
    {"type": "dimensions", "id": "D11", "description": "SET REFERENCE GUARD: Tableau Sets are converted into Calculated Columns returning 'In' or 'Out'. If you encounter a Tableau Set membership check like `[Column] IN [SetName]` or simply `[SetName]`, DO NOT output DAX using the `IN` operator against a column or measure name. Instead, you MUST rewrite the logic to check if the Set's calculated column evaluates to \"In\". Assume the Set's calculated column is named exactly `[SetName_Category]` or `[SetName_In_Out]`. For example, at the row level: `IF('Table'[Department Set_In_Out] = \"In\", ...)` or `IF('Table'[Department Set_Category] = \"In\", ...)`. NEVER use `IN [SetName]` directly in DAX."},
    {"type": "dimensions", "id": "D13", "description": "MEASURE AGGREGATION GUARD: If you are referencing a field that is listed as an 'Available Measure' or 'LOD EXPRESSION', you MUST NOT wrap it in SELECTEDVALUE(), SUM(), MAX(), or any other aggregator. Measures already return a scalar value and must be referenced directly as [MeasureName] even in row-level context. SELECTEDVALUE() is EXCLUSIVELY for physical table columns."},
    {"type": "dimensions", "id": "D14", "description": "TYPE CONVERSION & EXPLICIT CASTING: When a Tableau dimension explicitly converts a string to a date/time using functions like DATEPARSE(), DATE(), or DATETIME(), you MUST translate this to DAX using DATEVALUE() or VALUE(). Do not strip these explicit conversions. ISO 8601 TIMESTAMP EXCEPTION: If the format string in DATEPARSE() contains 'T' or 'HH:mm:ss', or the column contains ISO 8601 timestamp strings (e.g. '2014-12-14T20:14:30Z'), DAX's DATEVALUE() will throw an error if passed the full string. In DAX, you MUST extract the 10-character date portion first using LEFT('Table'[Column], 10) before wrapping it in DATEVALUE(). For example, DATE(DATEPARSE(\"yyyy-MM-dd'T'HH:mm:ss'Z'\", [Start])) becomes DATEVALUE(LEFT('Table'[Start], 10)). Similarly, map explicit numeric casts (INT, FLOAT) to DAX VALUE() or INT(). CRITICAL: When wrapping columns in these conversion functions, you MUST still respect the CROSS-TABLE REFERENCE GUARD rules! Apply the appropriate cross-table function INSIDE the conversion function, e.g., DATEVALUE(LEFT(RELATED('OtherTable'[Column]), 10)) for Many-to-One."},


    # -------------------------------------------------------------------------
    # MEASURES — Aggregations + Table Calculations — M1 to M8
    # -------------------------------------------------------------------------
    {"type": "measures", "id": "M1", "description": "STRICT QUALIFICATION & VAR USAGE: Resolve EVERY column to its exact DAX format 'TableName'[ColumnName] using the provided Schema Context. For each column reference, you MUST deduce the correct TableName from the schema — do not assume all columns belong to the target table. Measures must be referenced as [MeasureName] only. For all parameter references, ALWAYS store the SELECTEDVALUE('ParameterTable'[ParameterColumn]) in a VAR for clarity and performance. Use the EXACT Table and Column name specified in the Available Parameters context — for DATATABLE-based parameters, the column is ALWAYS named 'Value' (e.g., SELECTEDVALUE('ParameterName'[Value])). For DISTINCT-based parameters, use the column name specified in the schema context. NEVER use naked columns in scalar contexts — you MUST wrap the Fact/Dimension table column in SELECTEDVALUE() or MAX() when comparing it to a variable or parameter. Output clean DAX with ZERO comments."},

    {"type": "measures", "id": "M2", "description": "BASIC AGGREGATIONS, RATIOS & TOTALS: Map SUM, AVG, MIN, MAX, and COUNT to DAX. Ensure COUNT -> COUNT() and COUNTD -> DISTINCTCOUNT() on the FACT/transaction table column, NOT on the dimension/lookup table. STATISTICAL FUNCTIONS: Map Tableau STDEV -> STDEV.S, STDEVP -> STDEV.P, VAR -> VAR.S (never map to VAR in DAX as it is a reserved keyword for variables), and VARP -> VAR.P. ID FIELD RULE: When aggregating an ID or Key column (e.g. 'Encounter Id', 'Patient ID', or columns ending in 'Id'), prefer using DISTINCTCOUNT() over COUNT() to ensure unique counts, particularly in contribution/ratio measures. Use DIVIDE() for all division. Translate Tableau TOTAL([Measure]) to CALCULATE([Measure], ALLSELECTED()) to respect user filters. Do NOT use REMOVEFILTERS() for TOTAL()."},

    {"type": "measures", "id": "M3", "description": "SCALAR CONTEXT & AGGREGATION GUARD: In a Measure, you CANNOT use a bare column reference ('Table'[Column]) in a scalar comparison or function. You MUST wrap the naked column in SELECTEDVALUE() (e.g., SELECTEDVALUE('Table'[Column])) to satisfy the requirement for a scalar value. MEASURE QUALIFICATION RULE: If a name appears in the 'Available Measures' section of the schema context, it is a MEASURE — you MUST reference it as [MeasureName] ONLY, with NO table prefix. NEVER wrap a Measure inside SELECTEDVALUE(). If referencing a Measure (e.g., [MeasureName]), use it directly. SELECTEDVALUE() is EXCLUSIVELY for table columns. Never wrap a Measure inside an aggregator like SUM(), AVERAGE(), MIN(), MAX(), or COUNTROWS(). MEASURE AGGREGATION RULE: If you need to aggregate a measure across rows (e.g. Tableau SUM([Measure])), you MUST use iterator functions like AVERAGEX or SUMX. For standard measures, iterate over a granular dimension like VALUES('Table'[ID_Column]). RATIO ITERATION EXCEPTION: If aggregating a measure as the numerator of a ratio where the denominator is a distinct count of an ID (e.g., SUM([Measure]) / COUNTD([ID])), you MUST iterate over that exact same ID column using SUMX(VALUES('Table'[ID]), [Measure]) to ensure accurate row-by-row evaluation. FIXED LOD EXCEPTION: If the measure is a FIXED LOD, you MUST iterate strictly over the EXACT dimension(s) the LOD is fixed on (e.g., VALUES('Table'[FixedDim])). NEVER use a more granular ID column (like [Transaction ID] for a [Customer ID] LOD) as this causes double-counting. If the exact fixed dimensions of the LOD are UNKNOWN or missing from the schema context, DO NOT guess an ID column for SUMX (like a transaction [Id]) as this causes massive double-counting or forces the LOD to evaluate as 1.00. Instead, assume it is a global denominator: drop the SUMX entirely and use CALCULATE([LODName], ALLSELECTED()). EXCEPTION FOR TABLE-SCOPED FIXED LODS: If a FIXED LOD has no fixed dimensions (i.e. is table-scoped or fixed on nothing, listed with fixed_dims: [None (Table-scoped)]), you MUST NOT use an iterator function (like SUMX or AVERAGEX). Since it returns a global constant, any aggregation on it (SUM, AVG, MIN, MAX) in Tableau is simply to satisfy Tableau's aggregation mixing rules. In DAX, you must wrap it in CALCULATE with ALLSELECTED() to clear the visual's grouping and filter context, ensuring it evaluates as a global total. For example, SUM([Total Visits (LOD)]) becomes CALCULATE([Total Visits (LOD)], ALLSELECTED())."},

    {"type": "measures", "id": "M4", "description": "LOOKUP TRANSLATION (DYNAMIC TIME INTELLIGENCE vs POSITIONAL OFFSET): Convert Tableau LOOKUP into DAX by FIRST classifying the shift type, THEN generating code for that classification ONLY.\n\nSTEP 0 — MANDATORY CLASSIFICATION (do this BEFORE writing any DAX):\nAnalyze the Calculation Name, the LOOKUP offset value, and the schema context to classify the shift into EXACTLY ONE category:\n- YEAR-LEVEL: The measure name contains 'PY', 'Prior Year', 'Last Year', 'YoY', 'Year over Year', 'LY', or 'Previous Year' (case-insensitive). OR the offset magnitude maps to a yearly cycle (e.g., -12 for monthly-grain data, -4 for quarterly-grain data).\n- MONTH-LEVEL: The measure name contains 'PM', 'Prior Month', 'Last Month', 'MoM', 'Month over Month', 'LM', or 'Previous Month' (case-insensitive). OR the offset is -1 and the schema context suggests monthly grain.\n- DAY-LEVEL: The measure name contains 'Yesterday', 'Daily', 'Day', 'DoD', 'Daily Change', 'vs Yesterday' (case-insensitive). OR the offset is -1 and the dataset contains daily-grain date records.\n- CATEGORICAL: The measure name does NOT suggest any time period shift, AND the LOOKUP references a non-date ordering dimension (e.g., rank, category position). This is the ONLY case where OFFSET() should be used.\n- DEFAULT RULE: If the measure name is ambiguous and the offset is -1, default to DAY-LEVEL if the schema context represents daily transactional date logs, or MONTH-LEVEL if monthly grain.\n\nSTEP 1 — GENERATE DAX FOR THE CLASSIFIED TYPE ONLY:\n\n1. YEAR-LEVEL SHIFT: Store the YEAR() of MAX() date in a VAR (_CurrentYear). Return COALESCE(CALCULATE([BaseMeasure], REMOVEFILTERS('FactTable'[DateColumn]), FILTER(ALL('FactTable'), YEAR('FactTable'[DateColumn]) = _CurrentYear - <year_offset>)), 0). HARD CONSTRAINT: The FILTER condition MUST check YEAR() ONLY. NEVER include && MONTH() matching. NEVER use EDATE(). If EDATE() appears in year-level output, the conversion is WRONG. You MUST include REMOVEFILTERS('FactTable'[DateColumn]) to clear auto-date-time hierarchy filters.\n\n2. MONTH-LEVEL SHIFT: Store MAX() date in a VAR (_CurrentDate). Compute target month using EDATE(_CurrentDate, <month_offset>) in a second VAR (_TargetMonth). Return COALESCE(CALCULATE([BaseMeasure], REMOVEFILTERS('FactTable'[DateColumn]), FILTER(ALL('FactTable'), YEAR('FactTable'[DateColumn]) = YEAR(_TargetMonth) && MONTH('FactTable'[DateColumn]) = MONTH(_TargetMonth))), 0). HARD CONSTRAINT: The FILTER condition MUST check BOTH YEAR() AND MONTH(). NEVER filter YEAR() alone for month-level shifts. EDATE() is MANDATORY for month-level shifts. You MUST include REMOVEFILTERS('FactTable'[DateColumn]) to clear auto-date-time hierarchy filters.\n\n3. DAY-LEVEL SHIFT: Store MAX() date in a VAR (_CurrentDate). Compute target date using `_CurrentDate + <offset>` (e.g. `_CurrentDate - 1` for yesterday) in a second VAR (_TargetDate). Return COALESCE(CALCULATE([BaseMeasure], REMOVEFILTERS('FactTable'[DateColumn]), 'FactTable'[DateColumn] = _TargetDate), 0). HARD CONSTRAINT: The target date calculation MUST use simple addition/subtraction (e.g. `_CurrentDate - 1`). Do NOT use EDATE(), YEAR(), or MONTH() in the filter condition. You MUST include REMOVEFILTERS('FactTable'[DateColumn]) as a direct argument of CALCULATE to clear existing date filters.\n\n4. CATEGORICAL OFFSET: Generate DAX using OFFSET() coupled with SUMMARIZE() and ORDERBY() over the dynamically resolved ordering column. NEVER use CALCULATE + FILTER + date functions for categorical shifts.\n\nBANNED PATTERNS (will cause blank values):\n- NEVER use REMOVEFILTERS() paired with implicit boolean filters like YEAR(col) = val as direct CALCULATE arguments. This combination causes blank values.\n- NEVER use bare boolean predicates like YEAR(col) = val as direct CALCULATE arguments. Always wrap them inside FILTER(ALL(...), condition).\n\nSTEP 2 — SELF-VALIDATION BEFORE OUTPUT:\nAfter generating the DAX, verify these constraints. If any fail, REDO the generation:\n- Year-level output must NOT contain EDATE or MONTH().\n- Month-level output MUST contain EDATE AND both YEAR() and MONTH() in the filter.\n- Day-level output must NOT contain EDATE(), YEAR(), or MONTH(). It must use simple date subtraction (e.g., `_CurrentDate - 1`).\n- Categorical output must NOT contain YEAR(), MONTH(), or EDATE().\n- ALL outputs MUST be wrapped in COALESCE(..., 0).\n- Year-level, Month-level, and Day-level outputs MUST contain REMOVEFILTERS('FactTable'[DateColumn]) as a direct argument of CALCULATE.\n\n5. UNIVERSAL FORMATTING & RESOLUTION: Derive all table, date, and ordering column names purely from the provided schema context. Do not invent names."},

    {"type": "measures", "id": "M5", "description": "RANKING & BOOLEAN FILTERS: Map RANK_DENSE to RANKX. Use ALLSELECTED() as the iteration table, NOT ALL(), so rankings respect slicer/filter context. The iteration dimension MUST be the display/name column, NEVER an ID column. RANKX ARGUMENT ORDER IS STRICTLY POSITIONAL: RANKX(<table>, <expression>, [<value>], <order>, <ties>) — the 4th argument is ALWAYS the sort order (DESC or ASC) and the 5th argument is ALWAYS the tie-breaking rule (DENSE or SKIP). NEVER swap the order and ties arguments. Tie-breaker keywords DENSE, SKIP, DESC, ASC must be literal words with NO QUOTES. For ascending rank, filter out blanks. Boolean rank filters (Rank <= N) MUST be cast to integer: IF(Condition, 1, 0). Related rankings on the same data MUST iterate over the same dimension. CRITICAL: When the 2nd argument of RANKX is an aggregation (COUNT, SUM, AVG, MIN, MAX, etc.) and NOT a pre-existing measure, you MUST wrap it in CALCULATE() to trigger context transition (e.g., RANKX(..., CALCULATE(COUNT(...)), ...)). Measures referenced as [MeasureName] have implicit context transition and do not strictly require the wrapper, but aggregations on columns DO."},

    {"type": "measures", "id": "M6", "description": "SCRIPT_STR / PYTHON / R TRANSLATION: Extract the mathematical logic from embedded Python/R scripts and rewrite as pure DAX. Use CALCULATE([Measure], ALLSELECTED()) to compute global aggregates of a measure — NOT AVERAGE([Measure]) which is invalid. Compare the current row's measure directly using [MeasureName]. NEVER reference a measure as a column inside AVERAGE() or SELECTEDVALUE()."},

    {"type": "measures", "id": "M7", "description": "MONTH / DATE-PART SEMANTICS: DATEPART returns a NUMBER — map to MONTH(), YEAR(), DAY(). For measures, always wrap the date column in SELECTEDVALUE() (e.g., MONTH(SELECTEDVALUE('Table'[Date]))) to ensure accurate row-context evaluation if unaggregated. NEVER wrap in MAX() or MIN() unless the original Tableau formula explicitly aggregates it. DATENAME returns a TEXT name — map to FORMAT() with appropriate format string. Do NOT confuse DATEPART with DATENAME."},

    {"type": "measures", "id": "M8", "description": "VAR / RETURN SYNTAX: When using VAR in DAX, the RETURN keyword is MANDATORY before the final expression. Every VAR block MUST end with RETURN followed by the result expression. AVERAGE(), SUM(), MIN(), and MAX() ONLY accept physical column references — NEVER pass a VAR name or a Measure name like [MeasureName]. To aggregate a complex computed value (e.g. nested measures or LODs) across multiple rows, you MUST use iterator functions (e.g., AVERAGEX, SUMX) over VALUES('Table'[ID_Column]). For simple column-based cross-table aggregations, follow rule M12."},
    {"type": "measures", "id": "M9", "description": "COLUMN IDENTITY PRESERVATION: NEVER rename, alias, or translate column names. If a column is listed as 'PowerBIName (Source: TableauName)', the identity is 'PowerBIName'. You MUST use 'PowerBIName' ONLY in the DAX output. DO NOT include the '(Source: ...)' part or anything inside the parentheses—this is a mapping hint, NOT part of the name. Inclusion of the '(Source: ...)' string or the TableauName in the DAX is a CRITICAL FAILURE. Every column name in the DAX output MUST match the target identity from the Schema Context exactly—no synonyms, no abbreviations, no expanded forms. This applies to ALL columns without exception."},
    {"type": "measures", "id": "M10", "description": "RESERVED KEYWORD GUARD FOR VARIABLE NAMES: NEVER use DAX or Power BI reserved keywords as VAR names. The following words are STRICTLY FORBIDDEN as variable names: TRUE, FALSE, BLANK, NOT, AND, OR, IN, VAR, RETURN, EVALUATE, ORDER, BY, ASC, DESC, DEFINE, MEASURE, COLUMN, TABLE, ROW, DATATABLE, CURRENCY, DATETIME, DOUBLE, INTEGER, STRING, BOOLEAN, DENSE, SKIP, EARLIER, EARLIEST, VALUE, DAY, MONTH, YEAR, HOUR, MINUTE, SECOND, IF, SWITCH, CALCULATE, FILTER, ALL, ALLSELECTED, ALLEXCEPT, REMOVEFILTERS, VALUES, DISTINCT, SELECTEDVALUE, HASONEVALUE, HASONEFILTER, ISBLANK, ISCROSSFILTERED, ISFILTERED, ISEMPTY, UNION, INTERSECT, EXCEPT, SUMMARIZE, ADDCOLUMNS, SELECTCOLUMNS, TOPN, GENERATE, GENERATESERIES, CROSSJOIN, NATURALINNERJOIN, NATURALLEFTOUTERJOIN, TREATAS, USERELATIONSHIP, RELATED, RELATEDTABLE, LOOKUPVALUE, RANK, RANKX, ROWNUMBER, OFFSET, WINDOW, INDEX, PARTITION, MATCHBY, ORDERBY, ABS, CEILING, FLOOR, ROUND, ROUNDUP, ROUNDDOWN, TRUNC, SIGN, SQRT, POWER, MOD, DIVIDE, SUM, SUMX, AVERAGE, AVERAGEX, MIN, MINX, MAX, MAXX, COUNT, COUNTA, COUNTX, COUNTROWS, COUNTBLANK, DISTINCTCOUNT, DISTINCTCOUNTNOBLANK, PRODUCT, PRODUCTX, CONCATENATE, CONCATENATEX, FORMAT, LEFT, RIGHT, MID, LEN, UPPER, LOWER, TRIM, SUBSTITUTE, REPLACE, REPT, SEARCH, FIND, EXACT, FIXED, UNICHAR, UNICODE, CODE, COMBINEVALUES, CONTAINSSTRING, CONTAINSSTRINGEXACT, SELECTEDMEASURE, SELECTEDMEASURENAME, ISSELECTEDMEASURE, SELECTEDMEASUREFORMATSTRING. Instead, use descriptive, prefixed names such as _TopN, _RankVal, _FilterVal, _Result, _CurrentValue, etc."},
    {"type": "measures", "id": "M11", "description": "SET REFERENCE GUARD: Tableau Sets are converted into Calculated Columns returning 'In' or 'Out'. If you encounter a Tableau Set membership check like `[Column] IN [SetName]` or simply `[SetName]`, DO NOT output DAX using the `IN` operator against a column or measure name. Instead, you MUST rewrite the logic to check if the Set's calculated column evaluates to \"In\". Assume the Set's calculated column is named exactly `[SetName_Category]` or `[SetName_In_Out]`. For example, `IF(SELECTEDVALUE('Table'[Department Set_In_Out]) = \"In\", ...)` or `IF(MAX('Table'[Department Set_Category]) = \"In\", ...)`. NEVER use `IN [SetName]` directly in DAX."},
    {"type": "measures", "id": "M12", "description": "CROSS-TABLE RELATIONSHIP GUARD: Optimize DAX based on table relationships. (1) MANY-TO-ONE: If the target table is the 'Many' side and you reference a 'One' side column, use RELATED('OneTable'[Column]). (2) ONE-TO-MANY: If the target table is the 'One' side and you aggregate a 'Many' side column, prefer simple aggregations like SUM('ManyTable'[Column]) if the relationship is direct, or SUMX(RELATEDTABLE('ManyTable'), 'ManyTable'[Column]) if complex logic is needed. AVOID using SUMX(VALUES('ManyTable'[ID]), ...) for simple cross-table sums as it is unnecessarily complex; use RELATEDTABLE instead when context transition is required."},
    {
  "type": "measures",
  "id": "M13",
  "description": "CONDITIONAL AGGREGATION (IF-THEN IN MEASURES): When a Tableau measure uses an IF statement with a row-level condition (like comparing a Date) to return a column value (e.g., IF DATEDIFF('day', [Date], TODAY()) <= N THEN [Value] END), DO NOT translate this to a DAX IF() statement. A DAX measure cannot evaluate row-by-row without an iterator. You MUST explicitly aggregate the column using CALCULATE and translate the condition into CALCULATE filter arguments. For example, use CALCULATE(AGG('Table'[Value]), KEEPFILTERS('Table'[Date] >= TODAY() - N)). ALWAYS use KEEPFILTERS() around the filter condition so you do not accidentally overwrite existing visual filters. Use TODAY() to perfectly match Tableau's local date evaluation (unless the dataset is static and requires MAX('Table'[Date]) as a fallback, but this fallback must be applied consistently). CONSISTENCY GUARD: If a workbook contains multiple conditional aggregations sharing the same logical date condition structure (e.g., filtering on the last N days or months using High, Low, Avg, etc.), they MUST all use the exact same structural pattern (e.g., all CALCULATE or all ITERATOR) and the exact same reference date (all TODAY() or all MAX(Date)). Mixed patterns or reference dates for related measures in the same workbook are strictly prohibited. Prefer the CALCULATE with KEEPFILTERS pattern using TODAY() consistently."
},
    {
  "type": "measures",
  "id": "M14",
  "description": "TABLE CALCULATIONS (WINDOW_SUM, WINDOW_AVG, RUNNING_SUM): Power BI does not have direct WINDOW_ equivalents. You MUST translate Tableau WINDOW_ aggregations (especially moving windows) into DAX using CALCULATE with dynamic date variables. CRITICAL: Tableau WINDOW_ formulas (e.g. WINDOW_SUM(COUNTD([Client Id]), -11, 0)) DO NOT explicitly mention the date column. You MUST logically deduce the primary FACT or TRANSACTION date column from the schema context. STRICT OVERRIDE: If the schema contains 'account'[Date] and 'account'[Loan Date], you MUST ALWAYS use 'account'[Loan Date]. NEVER use 'account'[Date] for time intelligence because it is a text field! Furthermore, if the metric is COUNTD([Client Id]), map it strictly to the fact-level ID (e.g., 'disp'[Client Id (Disp)]) rather than the dimension table ('client'[Client Id]) to ensure correct cross-filtering. Offset Mapping: Since Tableau's offset is row-based (e.g., -11 includes the 11 previous + current = 12 total), you must map this to the true calendar offset in DAX (e.g., -11 becomes -12). Format: VAR _MaxDate = MAX('account'[Loan Date]) VAR _MinDate = EDATE(_MaxDate, <adjusted_offset>) RETURN CALCULATE(<AGG>, REMOVEFILTERS('account'[Loan Date]), 'account'[Loan Date] >= _MinDate, 'account'[Loan Date] <= _MaxDate). EOMONTH GUARD (CRITICAL): If you use EOMONTH() to compute month boundaries, you MUST use the correct offsets. For PREVIOUS MONTH: _MinDate = EOMONTH(_MaxDate, -2) + 1 and _MaxEOM = EOMONTH(_MaxDate, -1). For CURRENT MONTH: _MinDate = EOMONTH(_MaxDate, -1) + 1 and _MaxEOM = EOMONTH(_MaxDate, 0). NEVER use EOMONTH(_MaxDate, -1) + 1 as a start date paired with EOMONTH(_MaxDate, -1) as an end date — this creates an impossible range where start > end and will ALWAYS return BLANK."
},
    {
  "type": "measures",
  "id": "M16",
  "description": "TEXT-TO-DATE COMPARISON GUARD: If you are generating date filters (like KEEPFILTERS or DATESBETWEEN) and the schema context indicates that the target date column is formatted as 'string' or 'text', you MUST wrap the column references in DATEVALUE() to prevent type mismatch errors. For example: VAR _MaxDate = DATEVALUE(CALCULATE(MAX('Table'[TextDate]), ALLSELECTED('Table'))) VAR _MinDate = EDATE(_MaxDate, -12) RETURN CALCULATE(..., KEEPFILTERS(DATEVALUE('Table'[TextDate]) >= _MinDate), KEEPFILTERS(DATEVALUE('Table'[TextDate]) <= _MaxDate))."
},
    {
  "type": "measures",
  "id": "M15",
  "description": "ROLLING DATE WINDOW EXCEPTION: If a Tableau formula computes a trailing/rolling period using an IF statement comparing a date against a Table-scoped LOD like {MAX([Date])} (e.g., IF [Date] >= DATEADD('month', -12, {MAX([Date])})), you MUST convert this into a moving window in DAX. Do NOT apply Rule L3 (REMOVEFILTERS) to the {MAX()} in this specific context. Instead, capture the max date using: VAR _MaxDate = CALCULATE(MAX('Table'[Date]), ALLSELECTED('Table')) to ensure consistency across categorical visual groupings. Compute the start date: VAR _MinDate = EDATE(_MaxDate, -12). Then RETURN CALCULATE(AGG('Table'[Measure]), KEEPFILTERS('Table'[Date] >= _MinDate), KEEPFILTERS('Table'[Date] <= _MaxDate)). This ensures the engine fixes bad Tableau logic and outputs a proper moving window."
},

    # -------------------------------------------------------------------------
    # LOD EXPRESSIONS — Always converted to Measures/Columns — L1 to L5
    # -------------------------------------------------------------------------

    {"type": "lods", "id": "L1", "description": "STRICT QUALIFICATION: Resolve EVERY field inside { } to 'TableName'[ColumnName] using ONLY the provided schema context. CRITICAL: When mapping dimensions, ALWAYS prioritize columns representing descriptive labels or names over internal technical IDs or keys if both are provided in the schema context. Avoid using foreign key IDs from the fact table if a corresponding descriptive column exists in a dimension table. ID/KEY EXCEPTION: If the grouping column is an ID, Key, or Code column (e.g. ending in 'Id', 'ID', 'Key', 'Code'), you MUST map it to the Fact/Metric table if it natively exists there, rather than mapping it to a dimension table. Do not perform cross-table mapping for ID/Key grouping columns. For simple LODs, map to the specific table where the grouping dimension column resides. For nested LODs, you MUST defer to the Column Resolution Hierarchy defined in rule L5. Always rely on the dynamic schema context and relationship labels to map columns."},


    {
    "type": "lods",
    "id": "L2",
    "description": """FIXED MAPPING & GRAIN (RELATIONSHIP-AWARE FOR SIMPLE LODs ONLY): This rule applies ONLY to simple, non-nested FIXED LODs (a single { FIXED [Dim] : AGG } with no inner LOD). Do NOT apply this pattern to nested LODs — use rule L5 instead.

STEP 1 — RESOLVE THE FIXED DIMENSION & FACT TABLE:
Using the schema context, resolve the FIXED dimension to its exact qualified form (e.g., 'DimensionTable'[DimColumn]). Identify the Fact Table being aggregated.

STEP 2 — ANALYZE RELATIONSHIP PATHS:
Look at the 'AVAILABLE RELATIONSHIPS' in the schema context to find the path from the Fact Table to the Dimension Table.

CASE A: SINGLE TABLE RELATIONSHIP (Same Table)
If the Fact Table and Dimension Table are exactly the SAME table, or if the grouping dimension natively exists on the Fact Table, wrap the aggregation in CALCULATE with ALLEXCEPT. Format: CALCULATE(<AGG>, ALLEXCEPT('<FactTable>', '<FactTable>'[<DimColumn>])).
CRITICAL RULE: NEVER use ALLEXCEPT with a column that does not natively exist on that table in the schema.

CASE B: CROSS-TABLE RELATIONSHIP (Different Tables)
If the Dimension Table is different from the Fact Table (e.g., Dim -> Fact one-to-many relationship where Dimension is the 'One' side and Fact is the 'Many' side):
Do NOT use REMOVEFILTERS on the Fact Table, as this clears the filters propagating from the Dimension Table. Simply use ALLEXCEPT on the Dimension Table.
Format: CALCULATE(<AGG>, ALLEXCEPT('<DimensionTable>', '<DimensionTable>'[<DimColumn>])).
If the relationship is complex (Many-to-Many) or requires bridging, use RELATEDTABLE instead.
Format: CALCULATE(<AGG>, REMOVEFILTERS('<FactTable>'), RELATEDTABLE('<IntermediateBridgeTable>'), VALUES('<DimensionTable>'[<DimColumn>]))

STEP 3 — PARAMETER TABLE DETECTION:
After generating the base formula (Case A or Case B), inspect the provided JSON 'parameters' array.
Check if any parameter has 'powerbi.parameter_kind' equal to 'Dynamic Slicer (DISTINCT)'. Cross-reference the DAX field in 'powerbi.dax' to identify which dimension table and column the parameter table mirrors.
If the FIXED LOD's grouping dimension column exactly matches this mirrored column, the base filtering alone will be insufficient. You MUST append an additional argument inside your CALCULATE: TREATAS(VALUES('<pTableName>'[<DimColumn>]), '<DimensionTable>'[<DimColumn>]), where '<pTableName>' is the parameter table name and '<DimColumn>' is the column used.

CRITICAL: Resolve all table and column names dynamically from the JSON schema context. Do NOT hardcode any table or column names."""
    },

  {
    "type": "lods",
    "id": "L9",
    "description": "LOD SAFETY RULE (HARD OVERRIDE – EXECUTION PRIORITY): This rule MUST be applied BEFORE generating any aggregation (AVG, SUM, AVERAGEX, etc.). When converting Tableau FIXED LOD expressions, first analyze the inner expression inside the LOD (e.g., AVG([X])). You MUST classify [X] before generating DAX. Refer to the 'REFERENCED CALCULATED FIELDS (DEFINITIONS)' section in the schema context to find the underlying formula for [X]. If [X] is a base column, simple aggregation, or a simple count/sum LOD measure (like [Visits per Patient]), it is safe to reuse. In this case, you MUST reference it directly as [X] inside an iterator function (like AVERAGEX or SUMX) iterating over the fixed dimension of [X]. For example, AVG([Visits per Patient]) becomes AVERAGEX(VALUES('patients'[Id (patients)1]), [Visits per Patient]). If [X] is a derived or complex ratio expression (uses / or DIVIDE with multiple aggregations), it is NOT safe to reuse. In such cases, you MUST NOT generate any aggregation like AVERAGE([X]) or reuse the measure name. Instead, you MUST reconstruct the full logic of [X] using base columns at the level of the FIXED dimensions. CRITICAL: This rule OVERRIDES any direct aggregation mapping (such as AVG → AVERAGE). If [X] is a ratio, the 'average' of that ratio at the FIXED grain is the ratio itself calculated at that grain. CRITICAL ITERATOR RULE: NEVER use AVERAGEX, SUMX, or any iterator over the OUTER FIXED dimension (like Gender in `{ FIXED Gender : ... }`). FINAL RULE: If the expression is derived, the output MUST be rebuilt using base aggregations and MUST NOT contain the original measure reference."
},
    {"type": "lods", "id": "L3", "description": "EXCLUDE & TABLE-SCOPED: Map { EXCLUDE [Dim] : AGG } -> CALCULATE(AGG(), REMOVEFILTERS('DimTable'[ExcludedDim])). ALWAYS write COMPLETE function calls with ALL closing parentheses. Map Table-scoped LODs (e.g., {FIXED : AGG} or shorthand {AGG([Column])} like {MAX([Date])}) -> CALCULATE(<AGG>, REMOVEFILTERS('TableName')). CRITICAL: If an expression contains an embedded {MAX([Date])}, it MUST be translated as CALCULATE(MAX('Table'[Date]), REMOVEFILTERS('Table')) so it evaluates globally, ignoring the visual's filter context. Do not translate {MAX([Date])} as just MAX('Table'[Date])."},

    {"type": "lods", "id": "L4", "description": "INCLUDE MAPPING: Map { INCLUDE [Dim] : AGG } -> SUMX/AVERAGEX over VALUES('Table'[Dim]). Format exactly as: CALCULATE(<ITERATOR>(VALUES('Table'[Dim]), CALCULATE(<AGG>)))."},

    {"type": "lods", "id": "L5", "description": "NESTED LOD TRANSLATION: For nested FIXED LOD expressions, you MUST generate DAX using a nested iterator pattern. \n\nCOLUMN RESOLUTION HIERARCHY: When mapping dimensions to Iteration Functions (like VALUES), prioritize FACT/METRIC TABLE foreign keys or IDs over native DIM TABLE columns to preserve relationships.\n\nFORMULA GENERATION RULES:\n1) OUTER_DIMS: Establish the dimension(s) from the outermost FIXED expression.\n2) INNER_DIMS: Identify the unique dimensions in the inner FIXED expression that are NOT present in the outer expression (the dimension variance/difference).\n3) OUTER_ITERATOR: Identify the aggregation function wrapping the inner LOD block.\n4) INNER_MEASURE: The actual aggregation computed at the innermost level.\n\nOutput EXACTLY this nested structure (Use SUMMARIZE instead of VALUES if multiple dimensions are present at the level):\nSUMX(\n    VALUES(<ResolvedTable>[<OUTER_DIMS>]),\n    <OUTER_ITERATOR>(\n        VALUES(<ResolvedTable>[<INNER_DIMS>]),\n        CALCULATE(<INNER_MEASURE>)\n    )\n)\n\nCRITICAL: Do NOT simplify the formula by dropping the inner dimension logic. Both the outer aggregation and inner aggregation are MANDATORY for nested LODs. Do NOT use ALLEXCEPT here. Dynamically map Tables and Columns strictly based on the schema context provided."},
    {"type": "lods", "id": "L6", "description": "COLUMN IDENTITY PRESERVATION: NEVER rename, alias, or translate column names. If a column is listed as 'PowerBIName (Source: TableauName)', the identity is 'PowerBIName'. You MUST use 'PowerBIName' ONLY in the DAX output. DO NOT include the '(Source: ...)' part or anything inside the parentheses—this is a mapping hint, NOT part of the name. Inclusion of the '(Source: ...)' string or the TableauName in the DAX is a CRITICAL FAILURE. Every column name in the DAX output MUST match the target identity from the Schema Context exactly—no synonyms, no abbreviations, no expanded forms. This applies to ALL columns without exception."},
    {"type": "lods", "id": "L7", "description": "RESERVED KEYWORD GUARD FOR VARIABLE NAMES: NEVER use DAX or Power BI reserved keywords as VAR names. The following words are STRICTLY FORBIDDEN as variable names: TRUE, FALSE, BLANK, NOT, AND, OR, IN, VAR, RETURN, EVALUATE, ORDER, BY, ASC, DESC, DEFINE, MEASURE, COLUMN, TABLE, ROW, DATATABLE, CURRENCY, DATETIME, DOUBLE, INTEGER, STRING, BOOLEAN, DENSE, SKIP, EARLIER, EARLIEST, VALUE, DAY, MONTH, YEAR, HOUR, MINUTE, SECOND, IF, SWITCH, CALCULATE, FILTER, ALL, ALLSELECTED, ALLEXCEPT, REMOVEFILTERS, VALUES, DISTINCT, SELECTEDVALUE, HASONEVALUE, HASONEFILTER, ISBLANK, ISCROSSFILTERED, ISFILTERED, ISEMPTY, UNION, INTERSECT, EXCEPT, SUMMARIZE, ADDCOLUMNS, SELECTCOLUMNS, TOPN, GENERATE, GENERATESERIES, CROSSJOIN, NATURALINNERJOIN, NATURALLEFTOUTERJOIN, TREATAS, USERELATIONSHIP, RELATED, RELATEDTABLE, LOOKUPVALUE, RANK, RANKX, ROWNUMBER, OFFSET, WINDOW, INDEX, PARTITION, MATCHBY, ORDERBY, ABS, CEILING, FLOOR, ROUND, ROUNDUP, ROUNDDOWN, TRUNC, SIGN, SQRT, POWER, MOD, DIVIDE, SUM, SUMX, AVERAGE, AVERAGEX, MIN, MINX, MAX, MAXX, COUNT, COUNTA, COUNTX, COUNTROWS, COUNTBLANK, DISTINCTCOUNT, DISTINCTCOUNTNOBLANK, PRODUCT, PRODUCTX, CONCATENATE, CONCATENATEX, FORMAT, LEFT, RIGHT, MID, LEN, UPPER, LOWER, TRIM, SUBSTITUTE, REPLACE, REPT, SEARCH, FIND, EXACT, FIXED, UNICHAR, UNICODE, CODE, COMBINEVALUES, CONTAINSSTRING, CONTAINSSTRINGEXACT, SELECTEDMEASURE, SELECTEDMEASURENAME, ISSELECTEDMEASURE, SELECTEDMEASUREFORMATSTRING. Instead, use descriptive, prefixed names such as _TopN, _RankVal, _FilterVal, _Result, _CurrentValue, etc."},
    {"type": "lods", "id": "L8", "description": "SCALAR MEASURE CONTEXT GUARD: LOD expressions are created as Measures in Power BI. Therefore, ANY column reference outside of an iterator or aggregator function MUST be wrapped in SELECTEDVALUE(). Never compare an aggregate directly to a bare column like = 'Table'[Column]. Instead, you MUST use = SELECTEDVALUE('Table'[Column]). CRITICAL: This rule ONLY applies to Table Columns. NEVER wrap a Measure reference in SELECTEDVALUE(). If you refer to another Measure (e.g. an existing LOD measure), use it directly: [MeasureName]. NEVER output a naked column reference anywhere in the final DAX formula."},
    {"type": "lods", "id": "L10", "description": "LOD MEASURE SUMX EXCEPTION: If the FIXED LOD is created as a measure and is expected to be displayed in a KPI card, summary card, or grand total visual where it must match Tableau's visual-level SUM or AVG aggregation of that LOD field (e.g. named with suffixes like 'High', 'Low', 'Avg' or representing metrics like capacity, maximum, minimum, average over a dimension), you MUST write the measure using a SUMX or AVERAGEX iterator over the fixed dimension(s) instead of ALLEXCEPT. Example: SUMX(VALUES('Table'[Dim]), CALCULATE(<AGG>('Table'[Col]), REMOVEFILTERS('Table'[Date]))). NESTED LOD EXCEPTION: If the inner expression of the FIXED LOD aggregates a pre-existing LOD measure [LODName] (e.g. AVG([Visits per Patient])), you MUST NOT use rule L10. Instead, follow rule L9 and use the ALLEXCEPT pattern with the iterator over the inner LOD's fixed dimension (e.g. Patient ID), not the outer FIXED dimension. EXCEPTION: Do NOT use this rule if the LOD's aggregation is already SUM (e.g. {FIXED Customer: SUM(Amount)}). For SUM-based LODs, a simple ALLEXCEPT pattern (Rule L2) works perfectly. CRITICAL SAFETY GUARD: NEVER use REMOVEFILTERS() on a physical table or fact table inside CALCULATE within a SUMX/AVERAGEX iterator. Doing so completely clears context transition and relationship filters, causing the measure to return the global grand total for every row. Only use REMOVEFILTERS() on specific date or group columns that must be ignored for rolling calculations (e.g. REMOVEFILTERS('Table'[Date])). If there are no columns to ignore, omit the REMOVEFILTERS() argument entirely. CONSISTENCY GUARD: If a workbook contains multiple FIXED LODs sharing the same dimension grain (e.g., { FIXED Quote : MAX(Rate) }, { FIXED Quote : MIN(Rate) }, { FIXED Quote : AVG(Rate) }), you MUST translate all of them using the exact same structural pattern (either all ALLEXCEPT or all SUMX). Do not mix patterns. If any measure in the group requires SUMX (like 'Avg'), convert ALL measures in that group to SUMX."},
    {"type": "lods", "id": "L11", "description": "LOD TABLE-SCOPED DATE COMPARISON: If a FIXED LOD expression performs a row-level comparison of a Date column against a table-scoped max date (e.g. `IF Date = { FIXED : MAX(Date) } THEN Rate ELSE NULL END` or `IF Date = {MAX(Date)} THEN ...`), you MUST NOT use SELECTEDVALUE() for the column references. Instead, write the formula as a robust DAX measure using: `VAR _MaxDate = MAX('Table'[Date]) RETURN CALCULATE(AVERAGE('Table'[ValueColumn]), 'Table'[Date] = _MaxDate)`. This prevents blank values in Card or summary visuals."},
    {"type": "lods", "id": "L12", "description": "NESTED FIXED LOD AGGREGATION: When a Tableau FIXED LOD aggregates another pre-existing FIXED LOD measure (e.g., { FIXED [OuterDim] : AVG([InnerLOD]) } or { FIXED [OuterDim] : SUM([InnerLOD]) } where [InnerLOD] is fixed on [InnerFixedDim]), you MUST NOT iterate over the outer dimension. Instead, you MUST use the ALLEXCEPT pattern on the outer dimension, and wrap the inner measure in an iterator function (like AVERAGEX or SUMX) that iterates over the inner LOD's fixed dimension. MANDATORY PATTERN: CALCULATE(<Iterator>(VALUES('<InnerFixedDimTable>'[<InnerFixedDim>]), [InnerLOD]), ALLEXCEPT('<OuterTable>', '<OuterTable>'[<OuterDim>])). For example, { FIXED [Gender] : AVG([Visits per Patient]) } where [Visits per Patient] is fixed on patient ID becomes CALCULATE(AVERAGEX(VALUES('patients'[Id (patients)1]), [Visits per Patient]), ALLEXCEPT('patients', 'patients'[Gender]))."},
    {"type": "lods", "id": "L13", "description": "CROSS-TABLE FIXED LOD ON DIMENSION TABLE (Visits per Patient exception): When a Tableau FIXED LOD aggregates a column from a Fact Table (e.g. COUNT([Id]) on encounters) but is fixed on a column from a Dimension Table (e.g. [Id (patients)1] on patients), you MUST clear the filters on the Fact Table while preserving the Dimension column. Format: CALCULATE(COUNT('<FactTable>'[IdColumn]), REMOVEFILTERS('<FactTable>'), ALLEXCEPT('<DimensionTable>', '<DimensionTable>'[<DimColumn>])). For example, { FIXED [Id (patients)1] : COUNT([Id]) } becomes CALCULATE(COUNT('encounters'[Id]), REMOVEFILTERS('encounters'), ALLEXCEPT('patients', 'patients'[Id (patients)1]))."},

    {
       "type": "set_static_column",
        "id": "s4",
        "prompt": """Convert this Tableau static set into a Power BI Calculated Column.
        Set Name: {name}
        Table Name: {table}
        Field Name: {base_field}
        Members: {members_list}

        CRITICAL INSTRUCTIONS:
        - Return ONLY raw DAX.
        - Format: {name}_Category = IF('{table}'[{base_field}] IN {{ {members_list} }}, "In", "Out")
        - NO markdown, NO explanations."""
            },

     # --- 2. DYNAMIC SET RULE (TOP/BOTTOM N) ---
    # --- 2. DYNAMIC SET RULE (TOP/BOTTOM N) ---


    {
        "type": "set_dynamic_calculated_columns",
        "id": "s2_columns",
        "prompt": """You are a DAX architect converting a Tableau Dynamic Set (Top/Bottom N) into Power BI Calculated Columns.

        LOGIC: {mode} {count} on '{base_field}' (Order: {order})
        METRIC EXPRESSION: '{expression}'
        BASE TABLE: '{table}'
        SCHEMA: {schema_context}

        TASK:
        Generate a JSON object containing a list of "calculated_columns" required to replicate this set logic.
        You must generate three specific columns and explicitly define their data types:
        1. A "Metric" column: The aggregated calculation (e.g., Total Sales). Data type MUST be "Decimal Number".
        2. A "Rank" column: Using RANKX over the Base Table based on the Metric. Data type MUST be "Whole Number".
        3. An "In/Out" column: A boolean/text column using the Rank to determine if a row is "In" or "Out". Data type MUST be "text".

        STRICT DAX RULES:
        - All columns must be created on the Base Table: '{table}'.
        - Use fully qualified names: 'TableName'[ColumnName].
        - For the Metric column, if the data is in a related Fact table, use CALCULATE([Expression], RELATEDTABLE('FactTable')).
        - For the Rank column, use: RANKX(ALL('{table}'), '{table}'[MetricColumn], , {order}, DENSE).
        - For the In/Out column, use: IF('{table}'[RankColumn] <= {count}, "In", "Out").

        OUTPUT FORMAT:
        {{
            "calculated_columns": [
                {{"name": "Total_{base_field}", "dax": "...", "data_type": "Decimal Number"}},
                {{"name": "Rank_{base_field}", "dax": "...", "data_type": "Whole Number"}},
                {{"name": "{set_name}_In_Out", "dax": "...", "data_type": "text"}}
            ]
        }}"""
    },
    # --- 3. DYNAMIC CONDITION RULE ---
    {
        "type": "set_condition",
        "id": "s1",
        "prompt": """Convert this Tableau set condition to a DAX boolean expression.
        Tableau: '{expression}'
        Table: '{table}'
        Return ONLY the raw DAX condition. No extra text."""
    },
    {
   # --- 4. PARAMETER CONVERSION RULE ---
        "type": "parameter_to_powerbi",
                "id": "p1",
                "prompt": """Translate a Tableau Parameter into a Power BI DAX expression.
        Parameter Name: {name}
        Data Type: {data_type}
        Current Value: {current_value}
        Configuration: {configuration}

        TASK:
        1. If the configuration 'type' is 'List', generate a DAX DATATABLE exactly like this:
        {name} = DATATABLE("Value", [DAX_TYPE], {{{{ [VALUE1] }}, {{ [VALUE2] }}}})
        2. If it is 'All' or 'Range', generate a standard DAX measure or null as appropriate.
        3. [DAX_TYPE] must be INTEGER, STRING, or DOUBLE based on the data type.

        CRITICAL CONSTRAINTS:
        - Return ONLY the raw DAX code.
        - NO markdown, NO code blocks, NO explanations."""

    },

    # -------------------------------------------------------------------------
    # HEALTHCARE SPECIFIC MEASURES — MH1 to MH3
    # -------------------------------------------------------------------------
    {
        "type": "measures",
        "id": "MH1",
        "description": "DATEDIFF WITH TODAY() IN SUMX ROW CONTEXT (Aged Receivables / Days Since Pattern): When a Tableau formula uses DATEDIFF('day', [DateColumn], TODAY()) inside an IF or SUM to filter or aggregate rows based on how many days old a record is, you MUST translate this to DAX using SUMX or COUNTX with a FILTER over the table. MANDATORY PATTERN: SUMX(FILTER('Table', [StatusCol] = 'Status' && DATEDIFF([DateCol], TODAY(), DAY) > N), [AmountCol]). CRITICAL: DATEDIFF in DAX takes (StartDate, EndDate, Interval) — the order is REVERSED compared to Tableau which takes (interval, start, end). NEVER use DATEDIFF directly in SUM() — it requires row iteration via SUMX or FILTER. ALWAYS use TODAY() not NOW() when only date precision is needed."
    },
    {
        "type": "measures",
        "id": "MH2",
        "description": "PATIENT LIFETIME VALUE — MULTI-TABLE FIXED LOD SUM (Cross-table aggregation per patient): When a Tableau measure sums a column from one table and a column from another table both FIXED by [Patient Id] (e.g. {FIXED [Patient Id]: SUM([Total Amount])} + {FIXED [Patient Id]: SUM([Treatment Cost])}), you MUST NOT create two separate measures and add them. Instead, create a single measure using CALCULATE with ALLEXCEPT for each component, then sum them. MANDATORY PATTERN: VAR _Rev = CALCULATE(SUM('BILLS'[Total Amount]), ALLEXCEPT('BILLS', 'BILLS'[Patient Id (Bills)])). VAR _Cost = CALCULATE(SUM('TREATMENTS'[Treatment Cost]), ALLEXCEPT('APPOINTMENTS', 'APPOINTMENTS'[Patient Id])). RETURN _Rev + _Cost. CRITICAL: The ALLEXCEPT keys must match the FOREIGN KEY column name in each respective table — NOT a shared patient ID name. Cross-table LOD sums require separate ALLEXCEPT blocks per table."
    },
    {
        "type": "measures",
        "id": "MH3",
        "description": "BOOLEAN COLUMN FILTER IN COUNTD (Follow-Up Required, True/False column): When a Tableau formula uses COUNTD(IF [BooleanColumn] = TRUE THEN [ID] END) to count IDs where a boolean flag is set, translate to DAX using CALCULATE with DISTINCTCOUNT and a boolean filter. MANDATORY PATTERN: CALCULATE(DISTINCTCOUNT('Table'[ID Column]), 'Table'[BooleanColumn] = TRUE()). CRITICAL: In DAX, boolean columns from Snowflake are mapped as True/False type. Use TRUE() (with parentheses) NOT the literal 1 or 'true' string. NEVER use IF('Table'[BoolFlag] = TRUE(), ...) inside DISTINCTCOUNT — use it as a CALCULATE filter modifier instead. When dividing by total to get a rate, wrap in DIVIDE(..., DISTINCTCOUNT('Table'[ID Column]), 0)."
    },
    {
        "type": "measures",
        "id": "MH4",
        "description": "CROSS-TABLE FILTERING GUARD (One-to-Many COUNTD/Filter): When a Tableau measure aggregates a parent table (One side) ID but filters based on a child table (Many side) column (e.g., COUNTD(IF NOT ISNULL([ChildColumn]) THEN [ParentId] END)), you MUST shift the DISTINCTCOUNT to the child table. DO NOT use RELATED() or RELATEDTABLE() logic which will fail or hallucinate. MANDATORY PATTERN: If the formula is a ratio (like Follow-Up Completion Rate) dividing the filtered count by the total count, YOU MUST OUTPUT EXACTLY THIS DAX STRUCTURE without adding extra CALCULATE or COUNTROWS: DIVIDE(CALCULATE(DISTINCTCOUNT('ChildTable'[ParentId_ForeignKey]), 'ParentTable'[FilterCol] = TRUE(), NOT(ISBLANK('ChildTable'[ChildColumn]))), CALCULATE(DISTINCTCOUNT('ParentTable'[ParentId]), 'ParentTable'[FilterCol] = TRUE()), 0). For example, for Follow Up Completion Rate, output exactly: DIVIDE(CALCULATE(DISTINCTCOUNT('TREATMENTS'[Appointment Id (Treatments)]), 'APPOINTMENTS'[Follow Up Required] = TRUE(), NOT(ISBLANK('TREATMENTS'[Follow Up Date]))), CALCULATE(DISTINCTCOUNT('APPOINTMENTS'[Appointment Id]), 'APPOINTMENTS'[Follow Up Required] = TRUE()), 0)."
    },
    {
        "type": "measures",
        "id": "MH5",
        "description": "CATEGORICAL MONTH-OVER-MONTH (IGNORING YEAR): If a Tableau LOOKUP measure computes Month-over-Month growth (e.g., MoM %) but the data groups ALL years together (Categorical Month), DO NOT use EDATE or YEAR() filtering which restricts to a single year. Instead, you MUST use pure month math. MANDATORY PATTERN: VAR _CurrM = MONTH(MAX('Table'[DateCol])) VAR _PrevM = IF(_CurrM = 1, 12, _CurrM - 1) VAR _PrevVal = CALCULATE(SUM('Table'[Amount]), REMOVEFILTERS('Table'[DateCol]), FILTER(ALL('Table'), MONTH('Table'[DateCol]) = _PrevM)) VAR _CurrVal = CALCULATE(SUM('Table'[Amount]), REMOVEFILTERS('Table'[DateCol]), FILTER(ALL('Table'), MONTH('Table'[DateCol]) = _CurrM)) RETURN COALESCE(DIVIDE(_CurrVal - _PrevVal, ABS(_PrevVal), 0), 0)."
    },
    {
        "type": "measures",
        "id": "M17",
        "description": "CONTEXT PITFALL (MAX/MIN INSIDE FILTER EXPRESSIONS): NEVER use aggregation functions like MAX() or MIN() directly inside a row-iterating filter function like KEEPFILTERS() or FILTER() unless it is wrapped in CALCULATE(). Doing so evaluates MAX() in the row context, returning the current row's date instead of the global maximum, causing the filter to short-circuit. You MUST ALWAYS capture the MAX() value in a VAR before the CALCULATE() block and reference the VAR inside the filter condition."
    },
    {
        "type": "measures",
        "id": "M18",
        "description": "DATETRUNC GUARD & CURRENT/PREVIOUS MONTH CALCULATIONS: Power BI DAX does NOT natively support DATETRUNC in standard environments. When converting Tableau's DATETRUNC('month', [Date]) = DATETRUNC('month', MAX([Date])) for 'Current Month' or 'Previous Month' filtering, you MUST NOT use DATETRUNC. Instead, capture the MAX date in a variable, then use EOMONTH() math to define bounds. MANDATORY PATTERN for Current Month: VAR _MaxDate = CALCULATE(MAX('Table'[DateCol]), ALLSELECTED('Table')) VAR _MinDate = EOMONTH(_MaxDate, -1) + 1 VAR _MaxEOM = EOMONTH(_MaxDate, 0) RETURN CALCULATE(AGG(...), REMOVEFILTERS('Table'[DateCol]), 'Table'[DateCol] >= _MinDate, 'Table'[DateCol] <= _MaxEOM). MANDATORY PATTERN for Previous Month: VAR _MaxDate = CALCULATE(MAX('Table'[DateCol]), ALLSELECTED('Table')) VAR _MinDate = EOMONTH(_MaxDate, -2) + 1 VAR _MaxEOM = EOMONTH(_MaxDate, -1) RETURN CALCULATE(AGG(...), REMOVEFILTERS('Table'[DateCol]), 'Table'[DateCol] >= _MinDate, 'Table'[DateCol] <= _MaxEOM). CRITICAL: Ensure _MinDate is the 1st of the target month and _MaxEOM is the end of the target month."
    }
]
